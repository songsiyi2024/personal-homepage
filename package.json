{
  "name": "personal-homepage",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node tools/sync-shell.mjs",
    "check": "node tools/check-site.mjs",
    "validate": "npm run build && npm run check"
  }
}
